const num = 3; //num is an identifier
function multiplyBy2(inputNumber) { // multiplyBy2 is also an identifier
  const result = inputNumber * 2;
  return result;
}

const output = multiplyBy2(num);
const newOutput = multiplyBy2(10);
